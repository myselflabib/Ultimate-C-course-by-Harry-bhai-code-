// Date: 18/07/2024

#include<stdio.h>
int main(){
    int x = 2, y = 3, z = 3, k = 1;
    int e = 3*x/y - z + k;
    // 3*x/y - z + k
    // 6/y - z + k
    // 2 - z + k
    // -1 + k
    // 0
    printf("the value of the e is %d", e);
    return 0;
}